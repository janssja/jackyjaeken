export default function Loading() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-cream z-50">
      <div className="font-serif italic text-h3 text-gold animate-pulse">Jacky Jaeken</div>
    </div>
  );
}
